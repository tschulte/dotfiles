#!/bin/sh
#
# Usage: code-extension-backup
#
# Saves a list of your currently installed vscodium extensions to
# ~/.dotfiles/vscodium/extensions.txt suitable for install
# via code-extension-install

set -e

vscodium --list-extensions > ~/.dotfiles/vscodium/extensions.txt
