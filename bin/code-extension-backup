#!/bin/sh
#
# Usage: code-extension-backup
#
# Saves a list of your currently installed Code extensions to
# ~/.dotfiles/Code/extensions.txt suitable for install
# via code-extension-install

set -e

code --list-extensions > ~/.dotfiles/Code/extensions.txt
