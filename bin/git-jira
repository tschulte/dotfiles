#!/usr/bin/env sh
# show the jira issue details for the current branch
# assumes the branch name contains the jira issue id

git branch --show-current | sed -E "s/[^0-9]*([0-9]+).*/\1/" | xargs jira issue view

