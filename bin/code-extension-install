#!/bin/sh
#
# Usage: code-extension-install
#
# Installs the vscodium extensions listed in your extensions.txt file
# located at ~/.dotfiles/vscodium/extensions.txt
#
# You can generate a new list based on currently installed
# extensions via code-extension-backup

set -e

cat ~/.dotfiles/vscodium/extensions.txt | xargs -n1 vscodium --install-extension
