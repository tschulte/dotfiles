#!/bin/sh
#
# Usage: code-extension-install
#
# Installs the vscode extensions listed in your extensions.txt file
# located at ~/.dotfiles/vscode/extensions.txt
#
# You can generate a new list based on currently installed
# extensions via code-extension-backup

set -e

cat ~/.dotfiles/vscode/extensions.txt | xargs -n1 code --install-extension
